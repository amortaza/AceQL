bootstrap sch
does x_choice match bootstrap?
lookupRecords is duplicated

record.Initialize()
add "upload" to the list views
add "upload" to the form views
add created on
what happens when script does not exist
make front end aware of field types